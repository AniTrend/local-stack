version: '3'

services:
  traefik:
    container_name: "traefik"
    image: traefik:v3.0
    networks:
      - default
    ports:
      - 80:80
      - 443:443
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./logs:/var/log/traefik/
      - ./ssl-certs:/ssl-certs:ro
      - ./auth/htpasswd:/etc/htpasswd:ro
      - ./config/traefik.yml:/etc/traefik/traefik.yml:ro
      - ./config/dynamic.yml:/etc/traefik/dynamic.yml:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.traefik-ui.rule=Host(`${HOST}`)"
      - "traefik.http.routers.traefik-ui.entrypoints=web,websecure"
      - "traefik.http.routers.traefik-ui.tls=true"
      - "traefik.http.routers.traefik-ui.service=api@internal"
      - "traefik.http.routers.traefik-ui.middlewares=sso"
      - "traefik.http.middlewares.sso.plugin.trauth.domain=docker.localhost"
      - "traefik.http.middlewares.sso.plugin.trauth.cookiename=sso-cookie"
      - "traefik.http.middlewares.sso.plugin.trauth.users=${SSO_CREDENTIALS}"
      - "traefik.http.services.traefik-ui.loadbalancer.server.port=${PORT}"
      - "traefik.http.routers.traefik-ui.tls.domains[0].main=docker.localhost"
      - "traefik.http.routers.traefik-ui.tls.domains[0].sans=*.docker.localhost"
      - "traefik.tls.enable=true"
      - "traefik.tls.certresolver=${CERT_RESOLVER}"
      - "traefik.tls.certificates[0].certFile=/ssl-certs/localhost.crt"
      - "traefik.tls.certificates[0].keyFile=/ssl-certs/localhost.key"
    logging:
      options:
        max-size: "10m"
        max-file: "3"

networks:
  default:
    name: traefik
    external: true