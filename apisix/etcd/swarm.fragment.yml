services:
  etcd:
    networks:
      default:
        aliases:
          - apisix-etcd
    deploy:
      resources:
        reservations:
          memory: 64M
        limits:
          memory: 256M
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 0
        window: 120s
