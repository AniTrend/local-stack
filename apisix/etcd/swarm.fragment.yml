services:
  etcd:
    networks:
      default:
        aliases:
          - apisix-etcd
    deploy:
      resources:
        reservations:
          memory: 64M
        limits:
          memory: 256M
