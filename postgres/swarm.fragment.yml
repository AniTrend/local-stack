services:
  postgres:
    networks:
      default:
        aliases:
          - postgres
    deploy:
      resources:
        reservations:
          memory: 128M
        limits:
          memory: 512M
