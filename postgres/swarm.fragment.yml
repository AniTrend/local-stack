services:
  postgres:
    networks:
      default:
        aliases:
          - postgres
    deploy:
      resources:
        reservations:
          memory: 128M
        limits:
          memory: 512M
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 0
        window: 120s
