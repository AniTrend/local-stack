services:
  redis:
    networks:
      default:
        aliases:
          - redis
    deploy:
      resources:
        reservations:
          memory: 32M
        limits:
          memory: 128M
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 0
        window: 120s
