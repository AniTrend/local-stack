services:
  growthbook_proxy:
    networks:
      default:
        aliases:
          - growthbook-proxy
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      resources:
        reservations:
          memory: 64M
        limits:
          memory: 256M
