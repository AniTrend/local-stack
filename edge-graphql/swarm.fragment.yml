services:
  edge-graphql:
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      resources:
        reservations:
          memory: 96M
          cpus: '0.10'
        limits:
          memory: 384M
          cpus: '0.50'
