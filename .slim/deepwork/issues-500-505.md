# Deepwork: Issues 500-505

**Date**: 2026-06-26
**Base branch**: dev
**Status**: Phase 1 complete, awaiting oracle review

---

## Phase 0: Verification (COMPLETE)

All 6 issues independently verified against actual codebase. #505 deferred to separate session.

---

## Oracle Decisions (Phase 0)

| # | Topic | Decision |
|---|-------|----------|
| 1 | Phase ordering | GO as planned |
| 2 | --stacks on cmd_sync | ADD IT for consistency |
| 3 | Associative array | REMOVE IT (guard would break macOS entirely) |
| 4 | Docs naming | ALIGN DOCS TO CODE (remove docker-compose. prefix) |
| 5 | Prometheus APISIX target | FULLY QUALIFIED: infrastructure_apisix_gateway:9091 (cross-stack!) |
| 6 | Mongo volumes | SINGLE /data mount |
| 7 | #505 scope | SEPARATE SESSION |
| 8 | Risks | Runtime test needed for #501 (bash -n won't catch) |

---

## Phase 1: Implementation (COMPLETE)

### Issue 500 - fix/stackctl-sync-fail-closed
**Commit**: 8b02b51
**Changes**:
- `stackctl.sh:cmd_sync()`: Remove `|| true`, capture stderr to temp file, replay on failure
- Missing generated output now marked as DRIFT (was non-blocking "Warning")
- Added `-s/--stacks` flag with `set_target_stacks()` and `TARGET_STACKS` loop
- `stackctl.sh:_secrets_deploy()`: Removed `2>/dev/null` from generator call (line 1124)

**Validated**:
- `bash -n stackctl.sh` passes
- `./stackctl.sh sync --help` shows --stacks option
- Moving generate_stacks.py away → sync exits 1 with "Stack generation failed"
- `./stackctl.sh sync -s infrastructure` correctly scopes to one stack

### Issue 501 - fix/stackctl-bash-portability
**Commit**: f7978e9
**Changes**:
- `stackctl.sh`: Extracted `_dir_to_stack()` POSIX-safe helper function
- Replaced `local -A dir_to_stack=()` (Bash 4+ associative array) with linear scan
- Also removed `2>/dev/null` from generator call in `_secrets_deploy()`
- Zero `local -A` or `declare -A` remaining in script

**Validated**:
- `bash -n stackctl.sh` passes
- `grep -c 'local -A\|declare -A'` returns 0
- `./stackctl.sh env --list` works (20 discovered locations)
- `./stackctl.sh secrets deploy nonexistent-service` correctly reports error (no crash)

### Issue 504 - chore/mongo-volume-validation
**Commit**: e73ac17
**Changes**:
- `mongo/docker-compose.yaml`: Removed `mongo-data:/data/db` and `mongo-data:/data/configdb`
- Kept single `mongo-data:/data` mount with explanatory comment
- Generated stack (`generate_stacks.py --dry-run`) confirms single volume mount

---

## Phase 1 Branches

| Issue | Branch | Commit | Files Changed |
|-------|--------|--------|---------------|
| #500 | fix/stackctl-sync-fail-closed | 8b02b51 | stackctl.sh, .gitignore, .ignore |
| #501 | fix/stackctl-bash-portability | f7978e9 | stackctl.sh, .gitignore, .ignore, .slim/deepwork/* |
| #504 | chore/mongo-volume-validation | e73ac17 | mongo/docker-compose.yaml, .gitignore, .ignore |

---

## Phase 2 Plan (pending oracle review)

### Issue 503 - fix/prometheus-apisix-scrape-target
- Target: `apisix-gateway:9091` → `infrastructure_apisix_gateway:9091`
- File: `observability/prometheus/config/prometheus.yml:48`
- Decision: Fully qualified name (cross-stack DNS requires stack prefix)
- Also update `README.md:83` which lists "APISIX Gateway" under Prometheus scrape targets

### Issue 502 - docs/stackctl-canonical-workflow
- File: `stacks/README.md`
  - Fix rendered naming: `docker-compose.infrastructure.rendered.yml` → `infrastructure.rendered.yml` (lines 89-92)
  - Promote stackctl.sh section above raw docker stack deploy (move lines 47-84 above 22-45)
  - Label raw docker stack deploy as "Alternative (advanced)"
- File: `README.md`
  - Lines 194-205: Replace legacy docker-compose up commands with pointer to stacks/README.md
  - Line 8: Update quick-start pointer to mention stackctl.sh
