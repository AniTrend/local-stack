# Generated by tools/generate_stacks.py — do not edit manually.
volumes:
  beszel-data:
    external: true
    name: beszel-data
  beszel-socket:
    external: true
    name: beszel-socket
  grafana-data:
    external: true
    name: grafana-data
  loki-data:
    external: true
    name: loki-data
  prometheus-data:
    external: true
    name: prometheus-data
  tempo-data:
    external: true
    name: tempo-data
networks:
  default:
    name: traefik-public
    external: true
services:
  beszel:
    image: henrygd/beszel:0.18.7
    networks:
      - default
    volumes:
      - beszel-data:/beszel_data
      - beszel-socket:/beszel_socket
    env_file:
      - ./beszel/.env
    labels:
      - traefik.enable=${TRAEFIK_ENABLE}
      - traefik.http.routers.beszel.rule=Host(`${HOST}`)
      - traefik.http.routers.beszel.entrypoints=web,websecure
      - traefik.http.services.beszel.loadbalancer.server.port=${PORT}
      - traefik.http.routers.beszel.service=beszel
      - traefik.http.routers.beszel.tls=true
      - traefik.http.routers.beszel.tls.certresolver=${CERT_RESOLVER}
    logging:
      options:
        max-size: 10m
        max-file: 3
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      resources:
        reservations:
          memory: 64M
        limits:
          memory: 256M
  beszel-agent:
    image: henrygd/beszel-agent:0.18.7
    networks:
      - default
    volumes:
      - beszel-socket:/beszel_socket
      - /var/run/docker.sock:/var/run/docker.sock:ro
    env_file:
      - ./beszel/.env
    logging:
      options:
        max-size: 10m
        max-file: 3
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      resources:
        reservations:
          memory: 32M
        limits:
          memory: 96M
  grafana:
    image: grafana/grafana-oss:13.0.1
    volumes:
      - grafana-data:/var/lib/grafana
      - ./observability/grafana/config/provisioning:/etc/grafana/provisioning/
    env_file: ./observability/grafana/.env
    labels:
      - traefik.enable=${TRAEFIK_ENABLE}
      - traefik.http.routers.grafana.rule=Host(`${HOST}`)
      - traefik.http.routers.grafana.entrypoints=web,websecure
      - traefik.http.routers.grafana.service=grafana
      - traefik.http.routers.grafana.tls=true
      - traefik.http.routers.grafana.tls.certresolver=${CERT_RESOLVER}
      - traefik.http.services.grafana.loadbalancer.server.port=${PORT}
    networks:
      - default
    logging:
      options:
        max-size: 10m
        max-file: 3
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      restart_policy:
        condition: any
      resources:
        reservations:
          memory: 128M
        limits:
          memory: 512M
  loki:
    image: grafana/loki:3.7.1
    command: -config.file=/etc/loki/config.yaml
    volumes:
      - ./observability/loki/config.yaml:/etc/loki/config.yaml
      - loki-data:/loki
    env_file: ./observability/loki/.env
    networks:
      - default
    logging:
      options:
        max-size: 10m
        max-file: 3
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      restart_policy:
        condition: any
      resources:
        reservations:
          memory: 256M
        limits:
          memory: 800M
  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.150.1
    command:
      - --config=/etc/otel-collector/config.yaml
    volumes:
      - ./observability/otel/config.yaml:/etc/otel-collector/config.yaml
    env_file: ./observability/otel/.env
    labels:
      - traefik.enable=${TRAEFIK_ENABLE}
      - traefik.tcp.routers.otel.rule=HostSNI(`${HOST}`)
      - traefik.tcp.routers.otel.entrypoints=web,websecure
      - traefik.tcp.routers.otel.service=otel
      - traefik.tcp.routers.otel.tls=true
      - traefik.tcp.routers.otel.tls.certresolver=${CERT_RESOLVER}
      - traefik.tcp.services.otel.loadbalancer.server.port=${PORT}
    networks:
      - default
    logging:
      options:
        max-size: 10m
        max-file: 3
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      restart_policy:
        condition: any
      resources:
        reservations:
          memory: 128M
          cpus: '0.10'
        limits:
          memory: 384M
          cpus: '0.40'
  prometheus:
    image: prom/prometheus:v3.11.2
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
      - --storage.tsdb.retention.size=512MB
    env_file: ./observability/prometheus/.env
    volumes:
      - ./observability/prometheus/config:/etc/prometheus/
      - prometheus-data:/prometheus
    labels:
      - traefik.enable=${TRAEFIK_ENABLE}
      - traefik.http.routers.prometheus.rule=Host(`${HOST}`)
      - traefik.http.routers.prometheus.entrypoints=web,websecure
      - traefik.http.routers.prometheus.service=prometheus
      - traefik.http.routers.prometheus.middlewares=sso@docker
      - traefik.http.routers.prometheus.tls=true
      - traefik.http.routers.prometheus.tls.certresolver=${CERT_RESOLVER}
      - traefik.http.services.prometheus.loadbalancer.server.port=${PORT}
    networks:
      - default
    logging:
      options:
        max-size: 10m
        max-file: 3
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      restart_policy:
        condition: any
      resources:
        reservations:
          memory: 512M
          cpus: '0.25'
        limits:
          memory: 1200M
          cpus: '0.80'
  memcached:
    image: memcached:1.6.41
    environment:
      - MEMCACHED_MAX_MEMORY=64m
      - MEMCACHED_THREADS=2
    networks:
      - default
    logging:
      options:
        max-size: 10m
        max-file: 3
  tempo:
    image: grafana/tempo:2.10.5
    user: tempo
    command:
      - -config.file=/etc/tempo/config.yaml
    volumes:
      - ./observability/tempo/tempo.yaml:/etc/tempo/config.yaml
      - tempo-data:/var/tempo
    environment:
      - TEMPO_GID=10001
      - TEMPO_UID=10001
    env_file: ./observability/tempo/.env
    networks:
      - default
    logging:
      options:
        max-size: 10m
        max-file: 3
    depends_on:
      - memcached
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      restart_policy:
        condition: any
      resources:
        reservations:
          memory: 256M
        limits:
          memory: 600M
