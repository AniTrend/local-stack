# Generated by tools/generate_stacks.py — do not edit manually.
volumes:
  uploads:
    external: true
    name: growthbook_dashboard-uploads
networks:
  default:
    name: traefik-public
    external: true
services:
  django-server:
    image: ghcr.io/anitrend/anitrend:0.8.0
    command:
      - /usr/src/start.sh
    networks:
      - default
    env_file: ./anitrend/.env
    labels:
      - traefik.enable=${TRAEFIK_ENABLE}
      - traefik.http.routers.anitrend-graphql.rule=Host(`${HOST}`)
      - traefik.http.routers.anitrend-graphql.entrypoints=web,websecure
      - traefik.http.routers.anitrend-graphql.service=anitrend-graphql
      - traefik.http.routers.anitrend-graphql.tls=true
      - traefik.http.routers.anitrend-graphql.tls.certresolver=${CERT_RESOLVER}
      - traefik.http.services.anitrend-graphql.loadbalancer.server.port=${PORT}
    logging:
      driver: local
      options:
        max-size: 2m
        max-file: 3
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      resources:
        reservations:
          memory: 128M
          cpus: '0.10'
        limits:
          memory: 384M
          cpus: '0.50'
  edge-graphql:
    image: ghcr.io/anitrend/edge-graphql:0.1.8
    networks:
      - default
    env_file: ./edge-graphql/.env
    labels:
      - traefik.enable=${TRAEFIK_ENABLE}
      - traefik.http.routers.edge-graphql.rule=Host(`${HOST}`)
      - traefik.http.routers.edge-graphql.entrypoints=web,websecure
      - traefik.http.routers.edge-graphql.service=edge-graphql
      - traefik.http.routers.edge-graphql.tls=true
      - traefik.http.routers.edge-graphql.tls.certresolver=${CERT_RESOLVER}
      - traefik.http.services.edge-graphql.loadbalancer.server.port=${PORT}
      - traefik.http.services.edge-graphql.loadbalancer.healthcheck.path=/health
    logging:
      driver: local
      options:
        max-size: 10m
        max-file: 3
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      resources:
        reservations:
          memory: 96M
          cpus: '0.10'
        limits:
          memory: 384M
          cpus: '0.50'
  growthbook:
    command:
      - yarn
      - start:with-tracing
    image: growthbook/growthbook:4.4.0
    volumes:
      - uploads:/usr/local/src/app/packages/back-end/uploads
    env_file: ./growthbook/dashboard/.env
    labels:
      - traefik.enable=${TRAEFIK_ENABLE}
      - traefik.http.routers.growth-dashboard.rule=Host(`${DASHBOARD_HOST}`)
      - traefik.http.services.growth-dashboard.loadbalancer.server.port=${DASHBOARD_PORT}
      - traefik.http.routers.growth-dashboard.entrypoints=web,websecure
      - traefik.http.routers.growth-dashboard.service=growth-dashboard
      - traefik.http.routers.growth-dashboard.tls=true
      - traefik.http.routers.growth-dashboard.tls.certresolver=${CERT_RESOLVER}
      - traefik.http.routers.growth-api.rule=Host(`${DASHBOARD_API_HOST}`)
      - traefik.http.services.growth-api.loadbalancer.server.port=${API_PORT}
      - traefik.http.routers.growth-api.entrypoints=web,websecure
      - traefik.http.routers.growth-api.service=growth-api
      - traefik.http.routers.growth-api.tls=true
      - traefik.http.routers.growth-api.tls.certresolver=${CERT_RESOLVER}
    networks:
      - default
    logging:
      options:
        max-size: 10m
        max-file: 3
    healthcheck:
      test:
        - CMD
        - wget
        - http://localhost:${API_PORT}
      interval: 20s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      resources:
        reservations:
          memory: 256M
          cpus: '0.20'
        limits:
          memory: 800M
          cpus: '0.80'
  growthbook_proxy:
    image: growthbook/proxy:git-efec2de
    env_file: ./growthbook/proxy/.env
    networks:
      - default
    labels:
      - traefik.enable=${TRAEFIK_ENABLE}
      - traefik.http.routers.growth-proxy.rule=Host(`${HOST}`)
      - traefik.http.services.growth-proxy.loadbalancer.server.port=${PORT}
      - traefik.http.routers.growth-proxy.entrypoints=web,websecure
      - traefik.http.routers.growth-proxy.service=growth-proxy
      - traefik.http.routers.growth-proxy.tls=true
      - traefik.http.routers.growth-proxy.tls.certresolver=${CERT_RESOLVER}
    logging:
      options:
        max-size: 10m
        max-file: 3
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      resources:
        reservations:
          memory: 64M
        limits:
          memory: 256M
  anitrend-edge:
    image: ghcr.io/anitrend/on-the-edge:1.5.1
    networks:
      - default
    env_file: ./on-the-edge/.env
    labels:
      - traefik.enable=${TRAEFIK_ENABLE}
      - traefik.http.routers.anitrend-edge.rule=Host(`${HOST}`)
      - traefik.http.routers.anitrend-edge.entrypoints=web,websecure
      - traefik.http.routers.anitrend-edge.service=anitrend-edge
      - traefik.http.routers.anitrend-edge.tls=true
      - traefik.http.routers.anitrend-edge.tls.certresolver=${CERT_RESOLVER}
      - traefik.http.services.anitrend-edge.loadbalancer.server.port=${PORT}
    logging:
      options:
        max-size: 10m
        max-file: 3
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      resources:
        reservations:
          memory: 128M
          cpus: '0.10'
        limits:
          memory: 384M
          cpus: '0.50'
  website:
    image: ghcr.io/anitrend/website:0.4.2
    networks:
      - default
    env_file: ./website/.env
    labels:
      - traefik.enable=${TRAEFIK_ENABLE}
      - traefik.http.routers.website.rule=Host(`${HOST}`)
      - traefik.http.routers.website.entrypoints=web,websecure
      - traefik.http.routers.website.service=website
      - traefik.http.routers.website.tls=true
      - traefik.http.routers.website.tls.certresolver=${CERT_RESOLVER}
      - traefik.http.services.website.loadbalancer.server.port=${PORT}
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      resources:
        reservations:
          memory: 64M
        limits:
          memory: 256M
    logging:
      driver: local
      options:
        max-size: 10m
        max-file: 3
