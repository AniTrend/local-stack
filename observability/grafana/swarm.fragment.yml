services:
  grafana:
    networks:
      default:
        aliases:
          - grafana
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      restart_policy:
        condition: any
      resources:
        reservations:
          memory: 128M
        limits:
          memory: 512M
